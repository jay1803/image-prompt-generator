* Folder strucutre
- {{platform}}
  - {{modal}}
    - {{keywords_category}}
      - {{keyword}}.png
      - {{keyword}}.txt

File & Folder Naming: 
1. All use lower case;
2. DO NOT put `space` in the filename, use `_` to replace all spaces.
